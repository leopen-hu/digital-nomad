import { devServerUrl } from './paths'

export const APP_BOUND_ID = devServerUrl
  ? 'com.rest-now.dev'
  : 'com.rest-now.app'
export const MAIN_WINDOW = 'main-window'
export const REST_WINDOW = 'rest-window'
